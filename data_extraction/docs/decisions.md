# Decisions & deviations from the spec

Every change to definitions, thresholds, fallbacks or layout is recorded here. Config changes also bump
`configs/pipeline.yaml: version`. (Pipeline version 1 = the initial implementation, before any stage output existed.)

| ID | Date | Topic | Decision | Reason | Status |
|---|---|---|---|---|---|
| D-001 | 2026-09-12 | Project root | The repository is the working directory `…/crecere-audio-analytics/data_extraction/`, not a home-directory project folder (§5.1). | Owner's instruction: every file, cache, model and output must live inside the working directory. | applied |
| D-002 | 2026-09-12 | Data source | Audio was already on disk at `../raw_data/{audios_humanos_censurados,audios_ia_censurados}`. The `download` stage copies it (read-only source, sha256-verified) into `data/raw/{humanos,ia}/`; gdown from the Drive folder ids was kept as a fallback (removed in D-030). | Avoids Drive quota; the folder outside the project is only read, never written. | applied (100/100 copied, verified) |
| D-003 | 2026-09-12 | Filesystem confinement | uv cache → `.cache/uv` (`[tool.uv] cache-dir`); managed Python 3.12 → `.cache/python` via `UV_PYTHON_INSTALL_DIR` in `scripts/env.sh` (uv accepts it only as an env var); `HF_HOME`, `TORCH_HOME`, `XDG_CACHE_HOME`, `TMPDIR` forced under `.cache/` by the CLI at start-up; HF token stored in `.cache/huggingface/token` via `scripts/hf_login.sh`; `link-mode = copy`. | Confinement rule; copy link mode avoids hardlink/reflink issues on the project volume. | applied |
| D-004 | 2026-09-12 | E0 implementation details | Parameters not fixed by §7, added under `censor:`: frames below −60 dBFS never count as tone/noise; tone peak search ≥ 100 Hz; flatness measured on a 5-frame moving-average spectrum; runs of one type ≤ 20 ms apart are merged before the min-duration test; overlapping types resolved zeros > tone > noise; `censor_type = mixed` when ≥ 2 types each hold ≥ 20 % of censored seconds. Tone spans store `freq_hz`. "Median speech energy" (before VAD exists) = median power of frames above the energy floor. Noise-span edges are re-anchored on the strongest sample-level energy step within ±32 ms. Per-call censor JSON is an object (spans + summary + QC), not a bare list. | Single-frame white-noise flatness ≈ 0.56 would fragment bursts at a 0.5 threshold. The smoothing biased edges by ~20 ms (unit test at ±20 ms failed until edges were refined). `freq_hz` separates a censor beep from call-progress tones (Colombian ringback is 425 Hz). | applied, subject to H3 |
| D-005 | 2026-09-12 | E0 `level_dbfs` | The manifest `level_dbfs` from E0 is the RMS of non-censored frames above −50 dBFS (no VAD yet). E2 computes the §10.2 definition (RMS of speech frames), which supersedes it in canonical JSON and tables. | Manifest is written before VAD exists. | applied |
| D-006 | 2026-09-12 | E1 censor zeroing | Censor spans are zeroed before the high-pass and zeroed again after resampling. | The zero-phase filter and soxr ring into the zeroed spans; E3 hallucination control needs exact digital silence there. | applied |
| D-007 | 2026-09-12 | torch pin / Pascal check | `torch==2.11.*`, `torchaudio==2.11.*` (cu126 index), `torchcodec 0.11.x`. Verified: `torch.cuda.is_available()` True, capability (6, 1), cuDNN 9.10; the arch list is `sm_50 sm_60 sm_70 …` (**no literal `sm_61`**), but compute capability 6.1 executes sm_60 kernels: matmul, conv1d, conv2d, 4-layer bi-LSTM, InstanceNorm, STFT all ran on the 4 GB test GPU. The §5.3 gate is therefore implemented functionally (`cuda_kernels_ok`: an `sm_6x` ≤ 6.1 kernel set present + a cuDNN conv executes); the literal check is still printed. | torchaudio's newest cu126 wheel is 2.11; pyannote.audio 4.0.7 needs ≥ 2.8. The purpose of the sm_61 check ("no kernel image" errors) is met. | verified |
| D-008 | 2026-09-12 | ffmpeg (H1) | Not installed system-wide. Decoding uses soundfile (all 100 files are PCM_16 WAV); pyannote receives in-memory waveforms (§9-E4); faster-whisper receives numpy arrays. | `sudo apt install` writes outside the working directory, and nothing needs it. | pending verification that pyannote imports without FFmpeg libs |
| D-009 | 2026-09-12 | E5 turn operationalization | Speech stretches = regular diarization ∩ VAD − censor, plus word attachment (nearest stretch of the word's speaker within 0.5 s, else a new stretch). Consecutive stretches of one speaker with no other speaker's stretch starting in between form one *unit*; the backchannel test (≤ 1.0 s, ≤ 2 words) applies to units. So "Sí." followed by more speech of the same speaker takes the floor. "Starts while A holds the floor" is read literally (any short unit inside A's turn), with a switch `turns.backchannel_requires_overlap` (default `false`). | Literal §9-E5. Known consequence: a short answer ("Sí.") inside the agent's floor is a backchannel, so CLIENT→AGENT latency is measured only after substantive client turns. Revisit with data if it distorts dynamics. | applied |
| D-010 | 2026-09-12 | E6 masking | In role-annotation inputs, masking (`lexicons.yaml: agent_masking`) applies to all speakers' turns; in E7 transcripts, to AGENT turns only (§9-E7). | Roles are unknown at E6, and the same session later annotates E9 blind. | applied |
| D-011 | 2026-09-12 | E6/E7 role merge | E7 rebuilds turns at role level with the E5 algorithm: speakers mapped to the same role fuse, and accepted suspect turns move their words to the proposed role. Canonical turn ids are new; E5 ids kept in `source_turn_ids`. Extensions: `roles.schema.json: suspect_turns[].proposed_role`; review sheet column `accept_suspect` ("all" or turn ids); columns `n_turns`. A review row is stale if its proposed role no longer matches the annotation. | Otherwise one agent split into two clusters would create fake turn changes, latencies and interruptions; spec needs a way to express "accepted reassignment". | applied |
| D-012 | 2026-09-12 | E9 validation extensions | Beyond the §9-E9 rules: `ptp=true` ⇒ `final_outcome` matches `ptp_type_commitment`; `ptp=false` ⇒ `agent_recapped="na"`; `third_party_relation≠na` iff `third_party`; `first_anchor` = proposer of the earliest offer (or `none`); items/flags with `value=true` need turn_id + quote; milestone order enforced only for debt_mention ≤ first_offer ≤ ptp (identification unconstrained, since disclosure-before-identification must be codable); optional top-level `flags: [quote_verification_failed]` in pass A/B schemas (spec requires the flag; strict schemas had no place for it); annotations whose `codebook_sha256` differs from the current codebook are `stale`. | Makes D-over-L fields well defined; keeps compliance breaches codable. | applied |
| D-013 | 2026-09-12 | E10 metric details | Exact match over rows where ≥ 1 side is non-null and neither is censored (censored counted separately); exact-match bands keep ≥ 0.8 / flag ≥ 0.6 / drop; F1 = micro over (call, type) pairs; Spearman and F1 use the κ bands; undefined κ (one category only) → `flag`; L variables without gold judged by self-consistency (rerun vs first run), capped at `flag`; D-over-L variables inherit the worst decision of their inputs; D/A variables inherit `flag` when role accuracy (seconds-weighted) or WER differs by group by > 0.05, according to `depends` in `variables.yaml`; `fillers_per_min` kept only if hyp/ref filler ratio ∈ [0.8, 1.25] with ≥ 5 reference fillers; WER normalization: casefold, strip accents/punctuation and `[CENSURADO]`, remove thousands separators. New keys under `validation:`. | §9-E10 leaves these open. | applied |
| D-014 | 2026-09-12 | Table representation | `number / null / "censored"` → numeric column + `<name>_censored` bool; `bool / "na"` → string enum `"true"/"false"/"na"`; rubric `"NA"` → null. Extended columns: `final_outcome_rank`, `last_turn_role`, `asks_if_robot_lexicon`, `asks_for_human_lexicon`. Extra flags: `annotation_missing`, `stereo_downmixed`. Shares are fractions (0–1); `*_pct` and `script_checklist_score` are 0–100. | Parquet columns need one dtype; keeps "censored" and "na" distinguishable. | applied |
| D-016 | 2026-09-12 | **Censor definition (pipeline v2)** | E0 v1 flagged "tone" in 100/100 calls (3999 spans), zeros in 32 calls (124 runs), noise in 1. Signal analysis (samples of 120 spans per family): 1000 Hz spans (1626; median 0.77 s, p10 0.48 s, p90 1.30 s) have interpolated-frequency std 0.00 Hz, amplitude CV 0.000 and 2nd harmonic −106 dB → a **digital 1 kHz sine beep = the provider's censoring method**. Spans at 125–550 Hz (2335) drift ~9 Hz (p90 17 Hz), amplitude CV 0.33 and 2nd harmonic −22 dB → voiced speech (sustained vowels / fillers), concentrated in the human group — zeroing them would delete speech asymmetrically. 121/124 zero runs are > 0.1 s from any beep (8 at file start) → line dropouts/padding, not censoring. v2: `censor.active_types: [tone]`, `tone_freq_hz: [1000] ± 50 Hz`, `tone_max_freq_std_hz: 3.0`. Zeros, noise and non-beep tones are still detected and stored as `diagnostic_spans` (E2 SNR already excludes exact-zero frames). E0 → E2 re-run. | §9-E0: "if results look implausible → H3; tune thresholds; log it". Detection is no longer ambiguous (synthetic-sine statistics), so H3 listening is offered as optional confirmation (example spans in `docs/data_notes.md`). | applied (v2) |
| D-017 | 2026-09-12 | Smoke-test ordering (§5.5, §14) | The §5.5 smoke test was split: the E3 part (3 calls: VRAM < 4 GB, outputs written, readable Spanish) passed, and the E3 batch on all calls was started before H2. The E4/E5 part ("speakers look plausible", diarization RTF) runs on the same 3 calls (`data/interim/smoke_calls.txt`, `scripts/smoke_test.sh diarize turns`) **before** any E4 batch. | ASR does not depend on diarization; H2 (token) was pending. Only one GPU stage runs at a time. | applied |
| D-018 | 2026-09-12 | Human checkpoints H2 / H4 | H2: the reviewer provided a Hugging Face read token, stored only in `.cache/huggingface/token` (git-ignored; no git credential, nothing in the home directory), and accepted the terms of the three pyannote repos. H4: The reviewer approved `configs/codebook.md` v1 as drafted (only the status header changed to "approved"). | Blocking gates of §4. | recorded |
| D-019 | 2026-09-12 | E3 fallback order + rerun | The full E3 batch ran out of CUDA memory on `large-v3` for 4 short calls (C026 485 s, C034 100 s, C036 99 s, C075 141 s — unrelated to length), which D-015 sent to `large-v3-turbo` on CUDA. Mixing models would make ASR quality differ by call. The chain is now `large-v3`/cuda → `large-v3`/cpu → `large-v3-turbo`/cuda → `large-v3-turbo`/cpu, and the 4 calls were re-run with `--force`. The 96 other calls never fell back, so their outputs are unchanged by the new order. A retry on the GPU with `CT2_CUDA_ALLOCATOR=cuda_malloc_async` still ran out of memory (C034), so the 4 calls were transcribed with `large-v3` on CPU (C034 first, then C026/C036/C075 with the runtime override `extract asr --device cpu`) while diarization used the GPU. | One ASR model for all calls where possible; CPU int8 uses the same weights. | applied |
| D-020 | 2026-09-12 | Cache staleness across stages | Caching was keyed on configuration only, so a call regenerated upstream (e.g. the 4 re-transcribed calls) would not invalidate downstream outputs. E5 turns, E6 role inputs, E7 canonical and E8a–d now add a fingerprint (mtime + size) of their per-call upstream JSON files to the cache key. E0–E4 keep config-only keys: their outputs already exist, and changing their keys would force a full re-transcription. After forcing an E0–E2 rerun for a call, re-run E3/E4 for that call with `--force` too; E5+ then follows automatically. | Rule §1.9 idempotence without silently stale features. | applied |
| D-021 | 2026-09-12 | **E5 sentence-level speaker smoothing (pipeline v3)** | Diarization smoke test (C021, C025, C042; community-1 on GPU, RTF 0.09–0.15, 2 speakers each) showed two error types: (a) pyannote puts some whole agent sentences in the other cluster (C025 66.9–72.0 s, 138–150 s) — left to E6 `suspect_turns` + H5; (b) segment boundaries lag ~1 s, so the start of a sentence lands on the other speaker (C021 "Me alegra que lleguemos a este \| acuerdo de pago…"); 3.5–6.2 % of words were a minority speaker inside their own Whisper sentence. After §9-E5 step 1, a short minority run inside a sentence (ends at . ? ! … or a pause > 1.0 s; run ≤ 1.5 s, ≤ 40 % of the sentence's words, sentence ≥ 4 words; runs made only of `lexicons.yaml: backchannel_tokens` are never changed) is reassigned to the sentence's majority speaker. Every change is listed in `turns/<call_id>.json: smoothing[]`, and the reassigned spans are removed from the old speaker's diarized speech in E5 and E7 so they cannot form wordless turns. Keys under `turns:`; switch off with `sentence_smoothing: false`. | Word-level speaker errors create fake turns, latencies and interruptions that the whole-turn `suspect_turns` mechanism handles only one turn at a time. | applied (v3) |
| D-022 | 2026-09-12 | E6 role inputs show the whole call | `annotation.roles_render_full_call: true`: the role input keeps the §9-E6 speaker summary and "first 90 s / 20 turns" section, then appends all remaining turns under a separate header. | The diarization smoke test showed whole agent sentences clustered with the other speaker mid-call (C025 at 67 s and 138–150 s). The `suspect_turns` → H5 → `diar_corrected` mechanism can only fix turns the annotator can see. | applied (v3) |
| D-023 | 2026-09-12 | **Per-call diarization overrides for speaker collapse (pipeline v4)** | In 15 calls one community-1 speaker held ≥ 90 % of diarized speech (C055 87 %). C063 is genuine (only the agent speaks). `scripts/diarization_variants.py` ran on the GPU for the 15 calls with three variants: community-1 min 1 / max 3, community-1 `num_speakers=2`, 3.1 `num_speakers=2`. Each was scored with a text-marker heuristic (agent vs client phrases per sentence). The heuristic proved unreliable: C046 scored 1.0 while agent speech was only split in time, and C039 scored 0.0 while its client interjections were separated. **Decision rule used instead:** a variant is adopted only if its sentence-level speaker view (majority speaker per ASR sentence, agent-name masking applied) alternates agent and client consistently beyond the opening. Adopted in `configs/diarization_overrides.yaml`: C020 and C092 → `speaker-diarization-3.1`, `num_speakers=2`; C039, C055, C061, C067 → community-1, `num_speakers=2`. Not collapse (the client speaks little; every variant gives the same view): C066, C087. Not fixed by any variant (kept at default, roles annotated with low confidence, flagged for H5): C024, C028, C046, C069, C084, C085, C095. Previous outputs of the 6 calls are kept in `data/interim/diarization_variants/default_before_override/`. The override hash is added to the E4 cache key (only those calls go stale), and E5+ follow via D-020 fingerprints. | Spec §9-E4 has one pipeline for all calls, but a merged agent/client speaker makes every E5–E8 measure of that call meaningless. Per-call overrides stay auditable, and roles still go through H5. | applied (v4) |
| D-024 | 2026-09-12 | Human checkpoint H5 (role review) | The reviewer reviewed `data/interim/review/roles_review.csv` (231 rows, 100 calls) and reported that the proposed roles match and are reliable. At the reviewer's instruction Claude filled `approved=Y` in all rows. Asked separately about suspect turns, the reviewer chose "accept all": `accept_suspect=all` in the 45 rows that have suspect turns (74 turns, 42 calls). No `corrected_role`. The sheet as it was before filling is kept at `data/interim/review/.roles_review.before_H5.csv`. E7 ran on 100/100 calls (`diar_corrected` flag in 42). | Blocking gate §4 H5; the values were entered by Claude on the reviewer's explicit instruction. | recorded |
| D-025 | 2026-09-12 | **Codebook v2 + H4 re-approval (pipeline v5)** | E9 dev round on C002, C046, C056, C062, C081, C090 (pass A + B, codebook v1; all valid). Claude listed 14 ambiguities, each with a proposed rule (`data/interim/review/dev_round_codebook_questions.md`). The reviewer answered "sí" to all 14 and reported no disagreements with the dev codes. Codebook v2 adds exactly those rules, tagged `[v2-1]`…`[v2-14]`. New validator check: `contact_type ≠ account_holder ⇒ objections empty`, with a test. Since v2 only adds the rules the reviewer accepted, that answer is recorded as the H4 approval of v2. Dev annotations re-coded under v2 and re-validated. H7: The reviewer corrected the WER texts in place in `<call_id>.whisper.txt`. The files were renamed to `<call_id>.txt` (references) and the hypotheses regenerated from `interim/asr` (differences: 4 / 17 / 26 / 13 lines). E9 order: for each call, pass A is written before pass B, in batches of 10 with `annotate-validate` after each batch. | Spec §9-E9 dev iteration → v2 → H4 before the full run. | applied |
| D-026 | 2026-09-13 | Provisional E10/E11 before H6 and self-consistency rerun | After the E9 run on 100/100 calls, `objection-qa`, `validate` and `tables` were run with 0 gold rows (H6 pending) and 0 reruns. Without that data, every L variable is `flag` (keep 64 / flag 68 / drop 0), and the tables are provisional. The self-consistency rerun of the 20 gold calls (`annotations/rerun_pass_a/`) is not done in this session: spec §9-E9 requires a fresh session that has not read earlier outputs, and this session has written them. The reviewer starts it. After H6 and the rerun, re-run `validate` then `tables`. The blindness probe (accuracy 0.95) is reported as-is. | Keeps the pipeline complete end-to-end while the blocking human inputs are gathered; respects the fresh-session rule. | provisional |
| D-027 | 2026-09-13 | H6 limited to 1 of 20 gold calls | The reviewer annotated one gold call (C004) and provided the values directly; the CSV on disk was still empty, so Claude entered that row with the reviewer's values unchanged except `TRUE`/`FALSE` → `true`/`false` (backup `data/validation/.gold_annotations.before_C004.csv`). The reviewer decided not to annotate the other 19, for time reasons. Consequence, accepted: agreement metrics need ≥ 2 rows with variance, so the 28 gold-validated variables and the 4 variables derived from them stay `flag`, and no interpretive variable can reach `keep`. The self-consistency rerun (`rerun_pass_a`) runs in a separate Claude session started by the reviewer. `validate` + `tables` are re-run once it finishes. The other session ran `uv sync` concurrently, so pipeline commands wait for it to finish, to avoid a half-installed environment. | The reviewer's explicit choice on a blocking human gate (§4 H6). | recorded |
| D-028 | 2026-09-13 | **Minimum rows for reliability decisions (pipeline v6)** | The first final E10 run (1 gold row, 20 reruns) marked `ptp_amount` and `ptp_date` (exact match 1.0) and `objections.type` (F1 0.67) as `keep` from a single call. A one-row agreement says nothing about reliability. New key `validation.min_rows_decision: 10`: any gold or self-consistency metric computed on fewer paired rows is `flag` ("too few rows to decide"), never `keep` or `drop`. Self-consistency (n = 20) is unaffected. The unit test is `tests/test_validation_decide.py`. `validate` and `tables` were re-run. | §9-E10 leaves sample-size handling open; without a floor, a partial H6 would produce unfounded `keep` decisions. | applied (v6) |
| D-029 | 2026-09-13 | **Variables removed from `calls`; codebook v3 (pipeline v7)** | The reviewer reviewed `calls.csv` and chose to remove 21 columns: `codec`, `sample_rate`, `channels`, `censor_type`, `exclude`, `exclusion_reason`, `third_party_relation`, `message_left`, `delinquency_stage`, `days_past_due`, `days_past_due_censored`, `debt_amount_censored`, `overdue_amount_censored`, `ptp_amount_censored`, `ptp_days_until`, `ptp_channel`, `asks_if_robot`, `asks_for_human`, `asks_if_robot_lexicon`, `asks_for_human_lexicon`, `abrupt_hangup`. `non_payment_reason` + `non_payment_reasons_all` are merged into `non_payment_reasons` (list, primary first, `["not_stated"]` if none). `final_outcome` and `final_outcome_rank` are kept. Removed from `variables.yaml`, tables, validation (`EXTRACT`, self-consistency, `DERIVED_FROM`, gold columns), E7 canonical `audio`, E8c lexical outputs, the E0 summary/manifest (`censor_type`), tests and the spec. Consequences: a censored amount is now null in `calls` (`offers.amount_censored` stays); `ptp_strength` counts only a numeric `ptp_amount`; `non_payment_reasons` self-consistency uses set F1. Codebook v3 drops the fields, [v2-3] and the `abrupt_hangup` part of [v2-11] and consistency rule 4 (numbering kept), and adds rule 11 (`not_stated` never combined). **Annotation migration:** backup `data/interim/annotations/.before_D029/`; in pass A and rerun pass A the removed fields were deleted and reasons merged (all 120 files had the primary first in the list, and `not_stated` only with an empty list, so no kept value changed); in pass A, pass B and rerun `_meta.codebook_sha256` was updated from `d8af4dd2…` to `5f5a2a84…` (v3), with a line added to `_meta.notes` (files re-serialized with 2-space indentation; a check against the backup found 0 differences beyond the intended ones). `annotate-validate`: pass A 100/100, pass B 100/100, rerun 20/20 valid. Role annotations don't carry a codebook check and were not touched. **Kept on purpose:** `manifest.csv` keeps `codec`/`sample_rate`/`channels`/`exclude`/`exclusion_reason` (exclusion of unusable audio, stereo/sample-rate flags); `censor.mixed_min_share` and the `asks_if_robot`/`asks_for_human` lexicons stay in the config unused, because they are part of the E0 and ASR cache keys and removing them would make E0–E8 stale (re-transcription could change turn ids). Per-call interim JSONs (`censor`, `canonical`, `features/lexical`) keep the old fields until regenerated; editing them would break the D-020 fingerprints. `censor_type` was dropped from the existing `manifest.csv`. Gold CSV: the three removed columns were dropped (backup `data/validation/.gold_annotations.before_D029.csv`) and from `README_gold.md`. `validate` (keep 56 / flag 56 / drop 2; `non_payment_reasons` self-consistency F1 0.84 → flag) and `tables` were re-run: `calls` has 100 rows × 107 columns; the dropped variables are still `coercive_language` and `incoherent_responses`. `../initial_analysis` was not changed (the reviewer's instruction); its sections s01, s05, s07, s08 and s11 read removed columns. | Columns were constant, empty or not useful for the analysis. | applied (v7) |
| D-030 | 2026-09-13 | **Publication cleanup (pipeline v8)** | Before publishing the code in a public repository: personal and account names, hardware/driver identifiers, home-directory paths and filesystem details were removed or generalized in the spec, README, decisions, data notes, codebook and docstrings (the human reviewer is now "the reviewer"). The Google Drive folder ids and the gdown fallback were removed (`paths.raw_groups[*].drive_id`, `download.py`, dependency `gdown`; `uv lock` dropped gdown, beautifulsoup4, pysocks and soupsieve). The raw audio now sits in the parent folder: `paths.raw_source: ../data/raw`. The codebook text change moved its sha256 to `d35cf18e…`; `_meta.codebook_sha256` was updated in pass A, pass B and rerun (220 files, backup `data/interim/annotations/.before_D030/`, note added), and `annotate-validate` passed (100 / 100 / 20). Consequence: `paths.raw_groups` is part of the `download` stage hash, so the cache keys of every per-call stage (E0–E8) changed. Existing outputs and tables are unchanged, but running a stage again recomputes it; re-running E3/E4 could change turn ids, so they should not be re-run. The extraction's git history (personal e-mail, session links) is kept locally outside the repository; the public repository starts from a clean commit. Tests: 60 passed. | Nothing personal or access-granting in a public repository. | applied (v8) |
| D-015 | 2026-09-12 | E3 fallback chain | Per call: `large-v3`/cuda → `large-v3-turbo`/cuda → `large-v3-turbo`/cpu, on CUDA/OOM/device errors only; `fallback_used` + attempts stored per call and counted in `_stage.json`. | Spec order "fallback_model, then CPU". | applied |
